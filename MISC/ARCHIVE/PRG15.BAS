REM ROB BASSETT
REM PRG15

CLS

RANDOMIZE TIMER
N = INT(50 * RND + 1)
DO
   S = S + 1
   COLOR (S)
   PRINT "PICK A NUBER BETWEEN 1 AND 50"
   INPUT G
   C = C + 1
   IF G = N THEN PRINT "GOOD GUESS"
   IF G > N THEN PRINT "TOO HIGH"
   IF G < N THEN PRINT "TOO LOW"
   IF G = N THEN EXIT DO
LOOP
PRINT "IT ONLY TOOK YOU "; C; "TIMES"

